Place converted TensorFlow Lite models here.

Expected filenames (matching app code):
- minilm_model.tflite
- distilbert_model.tflite
- mobilebert_model.tflite

You can provide any subset; the app will load whichever is present.
If none are present, it will fall back to keyword logic.
